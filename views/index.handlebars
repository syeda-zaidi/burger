<div id="main" class="headerImg">
    <img src="../public/assets/img/burger-fries.jpg" alt="headerimg" height="300px" width="300px">
    <h1 id="first-heading"> Here is A Menu of Best Burgers In Town</h1>
</div>

<div class="container">
    <div class="row">
        <div class="col-md-6">
            <h4> To be devoured </h4>
            <ul>
                {{#each burgers}}
                    {{#unless devoured}}
                        <li>
                            {{burger_name}}
                        </li>
                    {{/unless}}
                {{/each}}
            </ul>

        </div>
    </div>

</div>